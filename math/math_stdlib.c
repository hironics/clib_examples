#include <stdio.h>
#include <stdlib.h>
/*

some of the math functions are existing in stdlib
*/

int main(int argc, char * argv[])
{
    int a = -15;
    printf("a is :%d\n", a);
    printf("abs(a) is :%d\n", abs(a));
    return EXIT_SUCCESS;
}
